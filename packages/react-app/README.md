# React + Typescript@4 + Storybook

The idea of the components here is to have atomic pure components that any dApp project can just grab and build from. We want this to be used as a React template library

## Install

```bash
yarn
```

## Storybook

```bash
yarn storybook
```
