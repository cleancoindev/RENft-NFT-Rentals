{
  "name": "@renft/monorepo",
  "version": "0.0.1",
  "keywords": [
    "ethereum",
    "react",
    "yarn",
    "marketplace",
    "whitelabel",
    "nft",
    "erc721"
  ],
  "private": true,
  "scripts": {
    "react-app:build": "yarn workspace @renft/react-app build",
    "react-app:eject": "yarn workspace @renft/react-app eject",
    "react-app:ipfs": "yarn workspace @renft/react-app ipfs",
    "react-app:start": "yarn workspace @renft/react-app start",
    "react-app:test": "yarn workspace @renft/react-app test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
